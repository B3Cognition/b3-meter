<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <!-- JSON logging: activated by json-logging, production, or kubernetes profiles -->
  <springProfile name="json-logging | production | kubernetes">
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
      <encoder class="ch.qos.logback.contrib.json.classic.JsonLayout">
        <jsonFormatter class="ch.qos.logback.contrib.jackson.JacksonJsonFormatter"/>
        <timestampFormat>yyyy-MM-dd'T'HH:mm:ss.SSS'Z'</timestampFormat>
        <appendLineSeparator>true</appendLineSeparator>
      </encoder>
    </appender>
    <root level="INFO">
      <appender-ref ref="JSON"/>
    </root>
  </springProfile>

  <!-- Human-readable console logging for local development -->
  <springProfile name="default | dev">
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
    <root level="INFO">
      <appender-ref ref="CONSOLE"/>
    </root>
  </springProfile>

</configuration>
