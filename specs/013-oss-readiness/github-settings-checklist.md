# T-011 — GitHub Repository Settings Checklist

**Executor:** human (GitHub.com UI)
**Precondition:** T-001 through T-010 complete — verify with `./gradlew spotlessCheck && ./gradlew build`
**Estimated time:** 20 minutes
**FR reference:** FR-010

---

## Pre-flight: confirm automation is done

Before touching GitHub, verify all automated tasks passed:

```bash
./gradlew spotlessCheck    # must exit 0
./gradlew clean build      # must exit 0
gitleaks detect --source . --config .gitleaks.toml   # must show "no leaks found"
```

---

## Step 1 — Repository metadata (Settings → General)

- [ ] **Description:** `Modern load testing platform — compatible with Apache JMeter JMX test plans`
- [ ] **Website:** (leave blank or point to docs once published)
- [ ] **Topics:** add all of the following:
  ```
  load-testing
  jmeter
  performance-testing
  java
  kotlin
  spring-boot
  grpc
  open-source
  ```

---

## Step 2 — Feature tabs (Settings → General → Features)

- [ ] **Discussions:** enabled ✓
- [ ] **Wiki:** disabled ✗
- [ ] **Issues:** enabled ✓ (default)
- [ ] **Projects:** your choice (can leave default)

---

## Step 3 — Default branch (Settings → Branches)

- [ ] Confirm default branch is `b3meter` (not `main`)

---

## Step 4 — Branch protection rule on `b3meter` (Settings → Branches → Add rule)

Branch name pattern: `b3meter`

- [ ] **Require a pull request before merging** — enabled
  - Required approvals: **1**
- [ ] **Require status checks to pass before merging** — enabled
  - Add status check: `build` (from CI workflow)
  - Add status check: `spotless-check` (from CI workflow)
  - **Require branches to be up to date before merging** — enabled
- [ ] **Do not allow bypassing the above settings** — enabled
- [ ] **Allow force pushes** — **disabled** ✗
- [ ] **Allow deletions** — **disabled** ✗

> Note: Status check names (`build`, `spotless-check`) must match the `jobs:` keys in `.github/workflows/ci.yml` exactly. Verify after first CI run.

---

## Step 5 — Security advisories (Settings → Security)

- [ ] Confirm "Security advisories" is enabled (auto-activated by `SECURITY.md` presence)
- [ ] Confirm "Dependency graph" is enabled
- [ ] Consider enabling "Dependabot alerts" for automated CVE notifications

---

## Step 6 — Go Public (final step — do this LAST)

- [ ] Settings → General → Danger Zone → **Change repository visibility → Public**
- [ ] Confirm the warning dialog
- [ ] Verify the repo is now accessible without authentication

---

## Post-publish verification

After making public, confirm:

- [ ] GitHub shows "Security policy" tab (from `SECURITY.md`)
- [ ] First CI run triggers on push and `spotless-check` + `build` jobs appear
- [ ] Branch protection rule shows `build` and `spotless-check` as required status checks
- [ ] Repository description and topics are visible on the public page
- [ ] Discussions tab is visible

---

## Rollback

If anything is wrong after going public:
- Revert to private immediately: Settings → General → Danger Zone → Make private
- Fix the issue, then re-publish

---

*Generated by cognitive squad build — 2026-03-29*
